class SellingCarToWendy {
  object = "test";
  price = 1000;


  propose() {
    assert(owner !== fromAddress && amount >= this.price);
    owner = fromAddress;
    transfert('test', 345);
  }

  getBalanceOfWendy() {
    return balances['test'];
  }
}

exports = SellingCarToWendy;
